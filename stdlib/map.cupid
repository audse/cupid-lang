use [k, v] map [k, v] {

	fun [v]
	get = self, k key => get (self, key)

	fun [map [k, v]]
	set = mut self, k key, v value => set (self, key, value)
	
	fun [v]
	remove = mut self, k key => remove (self, key)
	
	fun [int]
	length = self => length (self)
}