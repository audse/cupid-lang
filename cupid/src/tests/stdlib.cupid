*** Cupid Standard Library v0.1.0 ***

map [string, int] test = [
	a: 1,
	b: 2
]

log (test)
log (test.b)

type dictionary = [
	map [A, B] entries,
	fun insert,
	fun remove,
	fun keys,
	fun values
]

fun div = int a, int b => a / b
int c = div(10, 10)

log (c)