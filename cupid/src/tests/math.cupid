
type [v] iter = [
	array [v] entries,
	int i
]

use [v] iter {

	fun [int]
	size = _ => {
		for i, _ in self.entries => i + 1
	}

	# fun [v]
	# next = _ => {
	# 	self.i += 1
	# 	(self.entries).(self.i)
	# }
	# 
	# fun [v] 
	# peek = int amount => {
	# 	(self.entries).(self.i + amount)
	# }
	
}

iter [v: int] mut myiter = [
	entries: [10, 20, 30, 40, 50],
	i: 0
]

type number = [
	int,
	dec
]
