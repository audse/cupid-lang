
class Person:

    # constructor
    with: first_name(str), last_name(str), birthday(str) => {

    }

    # structure is explicitly organized

    get:
        full_name: '(first_name) (last_name)'

    functions:

        say_stuff = sentence(str 'Hello World') {
            write sentence # write to terminal
        }


const my_person = new Person with name='Audrey', birthday='5/23/99'
